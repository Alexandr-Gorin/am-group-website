SANITY_PROJECT_ID=your_sanity_project_id
SANITY_DATASET=production
# SANITY_API_TOKEN=  # only needed for private datasets

# Note: the Sanity read-only preview token (SANITY_PREVIEW_TOKEN) lives in
# server/.env — not here — so it never appears in the client-side JS bundle.
